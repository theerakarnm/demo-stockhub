/**
 * Tailwind CSS v4 - the whole theme lives in src/app/globals.css (`@theme`),
 * so there is no tailwind.config.ts. Content files are discovered automatically.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
