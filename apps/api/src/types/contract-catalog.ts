/**
 * Wire contract for catalog and listing endpoints.
 *
 * Task 5 reserves the module so Track C (Task 24) can add interfaces here
 * without touching the contract barrel again. It starts empty on purpose and
 * is re-exported from ./contract.ts from day one.
 */

export {};
